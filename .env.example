# Admin AI — Example .env.local
# Copy this file to `.env.local` and fill in values.

###############################################
# Required for Admin AI
###############################################

# OpenAI (admin agent calls)
OPENAI_API_KEY=

# Supabase (browser/client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# App admin bootstrap (used by middleware/server guards)
SUPER_ADMIN_EMAIL=

# Cloudinary (server SDK reads this)
# Format: cloudinary://<api_key>:<api_secret>@<cloud_name>
CLOUDINARY_URL=

###############################################
# Optional / Recommended
###############################################

# Model overrides (defaults are cost‑effective)
# OPENAI_TEXT_MODEL=gpt-5-mini
# OPENAI_VISION_MODEL=gpt-5-mini

# Local rate limiting backend
# Use Vercel KV if configured:
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# For local development without KV, enable in‑memory backend (dev only):
# KV_USE_MEMORY=1

# Logging / tracing toggles
# Disable client SSE traces (default: ON)
# NEXT_PUBLIC_ADMIN_AI_TRACE_DISABLED=1
# Disable server route/orchestrator logs (default: ON)
# ADMIN_AI_TRACE_DISABLED=1
# Extra debug logging (structure hints for unknown agent events, default: ON; set 0 to disable)
# DEBUG_LOGS=1
# Include sensitive data in traces (default: ON; set 0 to disable)
# ADMIN_AI_TRACE_SENSITIVE=1
# Enable image validator logs (default: OFF)
# IMAGE_VALIDATION_DEBUG=1
# Image URL/file validation debug (default: OFF; set 1 to see detailed URL/file checks)
# IMAGE_VALIDATION_DEBUG=1

# Optional user‑side OpenAI API key (not used by Admin AI)
# OPENAI_API_KEY_USER=

# Cloudinary temp upload settings
# CLOUDINARY_TEMP_PREFIX=temp-uploads/
# CLOUDINARY_TEMP_RETENTION_DAYS=7

# OpenAI runtime tuning
# OPENAI_MAX_RETRIES=3

# Database URLs (not required for Supabase app runtime)
# DATABASE_URL=
# DIRECT_URL=
