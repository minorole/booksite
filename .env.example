#############################
# Optional: Direct Postgres URLs
#############################
# Only needed for external tooling or scripts. The app uses Supabase client APIs.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public
# DIRECT_URL=postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public

#############################
# OpenAI
#############################
# Where to find: https://platform.openai.com → Dashboard → API keys → Create new secret key
OPENAI_API_KEY=sk-...
OPENAI_API_KEY_USER=sk-...

#############################
# Supabase Auth (client + server)
#############################
# Where to find URL: Supabase Dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Where to find anon key: Supabase Dashboard → Project Settings → API → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

#############################
# App configuration
#############################
# Super admin email (choose an email you own; used by auth callback checks)
NEXT_PUBLIC_SUPER_ADMIN_EMAIL=admin@example.com

#############################
# Cloudinary (image uploads)
#############################
# Where to find URL: Cloudinary Dashboard → Account Details → API Environment variable (copy the cloudinary://... string)
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# Alternatively set individual values (Dashboard → Account Details)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=1234567890
# CLOUDINARY_API_SECRET=abcdefghijklmnopqrstuvwxyz

#############################
# Optional: Next.js environment
#############################
# NODE_ENV=development

#############################
# Optional: Upstash Redis (rate limiting / queues)
#############################
# Where to find: Upstash Console → Redis Database → Details → REST API
UPSTASH_REDIS_REST_URL=https://us1-balanced-bison-12345.upstash.io
UPSTASH_REDIS_REST_TOKEN=eyJrIjoi...
