## Summary

Describe the change and why it’s needed.

## Checklist

- [ ] CHANGELOG updated under `## Unreleased` with appropriate section (`Added`/`Changed`/`Fixed`/`Removed`/`Docs`/`Ops`/`Performance`).
- [ ] Audience tag(s) added (e.g., `[Users]`, `[Admin]`, `[Super Admin]`, `[DB]`, `[OpenAI]`, `[Security]`).
- [ ] Paths/envs referenced (workspace‑relative paths and env keys where relevant).
- [ ] Verification documented (how you validated locally or in CI).
- [ ] Tests added/updated if applicable.
- [ ] No secrets committed; `.env.local` not modified.

If this PR does not require a changelog entry, explain why (for example: docs-only or internal refactor):

> skip-changelog rationale:

## Changelog entry (copy here for convenience)

Section: Added | Changed | Fixed | Removed | Docs | Ops | Performance

Audience: [Choose one or more]

Summary: One line that explains the change and its impact.

Paths/Env: `src/...`, `.env.example`, etc.

